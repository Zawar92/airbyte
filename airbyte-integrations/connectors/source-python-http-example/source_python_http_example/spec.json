{
  "documentationUrl": "https://docs.airbyte.io/integrations/sources/coronaapi",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Corona Spec",
    "type": "object",
    "required": ["state"],
    "additionalProperties": false,
    "properties": {
      "state": {
        "type": "string",
        "description": "State information requested from the API.",
        "pattern": "^[a-z0-9_\\-]+$",
        "examples": ["states"]
      }
    }
  }
}
